export interface Settings {site_name:string; power_limit_kw:number; phase_limit_a:number; reserve_kw:number; rotation_seconds:number; paused:boolean}
export interface Simulation {building_kw:number;profile:'balanced'|'uneven';meter_online:boolean;disconnected:string[];offline:string[]}
export interface Station {id:string;name:string;model:'P30 x'|'P40';max_current_a:number;priority:'normal'|'high';paused:boolean;host:string;port:number;device_id:number;connected:boolean;online:boolean;network_online?:boolean;phases:number[];currents_a?:number[];current_a:number;power_kw:number;session_kwh:number;energy_kwh?:number;serial?:number|null;firmware_raw?:number|null;error_code?:number|null;web_url?:string;connection_detail?:string;status:'charging'|'paused'|'available'|'safe'|'waiting'|'offline'|'setup'}
export interface State {mode:string;status:string;timestamp:number;meter_timestamp:number;site_name:string;settings:Settings;stations:Station[];simulation:Simulation;building_kw:number;charging_kw:number;total_kw:number;phase_currents_a:number[];headroom_kw:number;meter_online:boolean;revision:number;overload:boolean;monta_status:string;commissioned:boolean}
export interface Sample {ts:number;building_kw:number;charging_kw:number;total_kw:number}
export interface Event {id:number;ts:number;level:string;message:string}
